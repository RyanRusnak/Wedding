<h1>Listing participants</h1>
<div id= "wrapper">
	
<ul class="partyPicGroom">
<%  Participant.find_each(:start =>1) do |participant| %>
  <li>
	<figure>
    <%= link_to (image_tag participant.picture), participant %>
    <%= participant.name %></br><%= participant.title %>
	<figure>
  </li>
<% break if participant.id > 9; %>

<% end %>
</ul>


<ul class="partyPicBride">
<%  Participant.find_each(:start =>11) do |participant| %>
 <li>
  <figure>
    <%= link_to (image_tag participant.picture), participant %>
    <%= participant.name %></br><%= participant.title %>
  <figure>
	</li>
<% end %>
</ul>

</div>